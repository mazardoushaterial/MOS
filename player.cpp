#include "player.h"
/*
Player::Player(Game* pGame)
{
    game = pGame; //Set the pointer to the current game
    sprite.setTexture(game->resourceManager.tileTextures);
    sprite.setTextureRect(sf::IntRect(0,0,50,75));
}
*/
